# 仓库配置说明

## 仓库概述
这是一个基于 GitHub Pages 和 Jekyll 构建的个人博客网站，使用 Minimal Mistakes 主题。

## 目录结构
```
firelifes.github.io/
├── _posts/              # 博客文章存放目录
├── _includes/           # 自定义模板覆盖目录
│   └── masthead.html    # 自定义顶部导航栏模板
├── _layouts/            # 自定义布局
│   └── category.html    # 分类归档页布局（支持空分类）
├── _data/               # 数据文件
│   └── ui-text.yml      # 中文翻译
├── assets/
│   ├── css/
│   │   └── main.scss    # 自定义样式文件（固定导航等）
│   └── images/
│       └── cover/       # 文章封面图目录
├── pages/               # 静态页面
│   ├── category-项目.md  # 项目分类页
│   ├── category-知识.md  # 知识分类页
│   └── category-随笔.md  # 随笔分类页
├── _config.yml          # 站点配置文件
├── index.md             # 首页
├── about.md             # 关于页面
├── categories.md        # 全部分类汇总页（更多）
├── search.md            # 搜索页面
└── README.md
```

## 自定义布局配置

### 小米汽车设计风格
- 主题色：卡布里蓝 (#2E86DE) - 灵感源自小米 SU7 的经典配色
- 辅助色：青绿色 (#4ECDC4) - 营造清新科技感
- 配色风格：通透、现代、富有科技感
- 使用 "air" 皮肤配合自定义样式

### 固定顶部导航栏
- 使用 CSS `position: fixed !important` 确保在所有页面固定
- 设置 `z-index: 2000` 和 `width: 100%` 确保覆盖
- `html { scroll-padding-top }` 和 `body { padding-top !important }` 双重保障内容不被遮挡
- 左侧显示：站点标题（加粗 + 文字阴影）+ 副标题
- 右侧显示：导航菜单（项目、知识、随笔、关于、更多）+ 搜索按钮
- 导航项悬停时显示渐变下划线动画
- 响应式设计：在小屏幕上垂直排列

### 文章 URL 结构
- 文章固定链接：`/category/:categories/:year/:month/:day/:title.html`
- 示例：`/category/随笔/2025/12/31/my-first-blog.html`
- 与分类归档页 `/category/随笔/` 形成层级关系

### 分类页面
- 三个分类页（项目/知识/随笔）使用自定义 `category` 布局
- 有文章时：显示文章列表，标题带蓝色装饰条 + 文章计数
- 无文章时：显示空白状态提示（"该分类下暂无文章，敬请期待"）
- 分类页永久存在，不会因为无文章而 404
- jekyll-archives 自动生成已关闭，改为手动管理

### 整体视觉优化
- 按钮：蓝色渐变 + 阴影 + 悬停上浮效果
- 链接：蓝色主题 + 底划线下划线动画
- 页脚：深色渐变背景
- 文章卡片：悬停时背景变化 + 位移动画
- 减少动画模式支持

### 中文本地化
- 创建 `_data/ui-text.yml` 翻译主题界面文本
- 设置 `locale: "zh-CN"` 启用中文界面
- 翻译内容：首页标签、导航、搜索、分页等

### 自定义文件：
- `_includes/masthead.html` - 导航栏模板
- `_layouts/category.html` - 分类页布局
- `assets/css/main.scss` - 自定义样式
- `index.md` - 首页内容美化
- `_config.yml` - 主题配置
- `_data/ui-text.yml` - 中文翻译文件
- `pages/category-*.md` - 三个分类页面

## 配置文件说明

### _config.yml
主要站点配置，包含：
- 站点标题、名称、描述
- 作者信息（头像、简介、社交链接）
- 导航栏配置
- 插件配置（jekyll-feed、jekyll-archives、jekyll-include-cache）
- 默认文章布局配置

## 如何写新博客

### 1. 文章文件位置
所有博客文章必须放在 `_posts/` 目录下。

### 2. 文章文件名格式
文件名必须遵循格式：`YYYY-MM-DD-文章标题.md`
- 示例：`2026-05-08-my-first-blog.md`

### 3. 文章头部 Front Matter
每篇文章开头必须包含以下元数据：
```yaml
---
layout: post
title: "文章标题"
date: YYYY-MM-DD HH:MM:SS +0800
categories: 知识
tags: [标签1, 标签2]
header:
  image: /assets/images/cover/your-cover.jpg
  caption: "图片来源说明"
toc: true
toc_label: "目录"
---
```

| 字段 | 必填 | 说明 |
|------|------|------|
| `layout: post` | ✅ | 固定值 |
| `title` | ✅ | 文章标题，建议加双引号 |
| `date` | ✅ | 发布时间，建议带 `+0800` 时区 |
| `categories` | ✅ | 三选一：`项目` / `知识` / `随笔` |
| `tags` | 可选 | 标签列表，如 `[投资, FIRE]` |
| `header.image` | 推荐 | 文章主图路径，放在 `assets/images/cover/` |
| `header.caption` | 可选 | 图片下方说明文字 |
| `toc: true` | 可选 | 开启右侧目录导航 |
| `toc_label` | 可选 | 目录标题文字 |

### 4. 文章内容
- 使用 Markdown 格式编写
- **第一段建议用 `>` 引用块** 作为摘要，会显示在文章列表
- 用 `##` 和 `###` 组织层级，右侧自动生成目录
- 段落间留空行，提升阅读体验
- 可参考仓库根目录的 **`博客模板.md`** 复制使用

### 5. 分类管理
- 站点共有 5 个主要分类：项目、知识、随笔、关于、更多
- 在文章的 `categories` 字段中指定分类（项目/知识/随笔）
- 分类会自动归档到对应页面
- 导航栏说明：
  - 项目：展示项目类文章
  - 知识：展示知识类文章
  - 随笔：展示随笔类文章
  - 关于：关于页面
  - 更多：显示全部分类（三横线图标）

## 资源文件

### 图片存放
- 文章封面图：`assets/images/cover/`
- 其他图片：可放在 `assets/images/` 下的对应子目录

## 部署方式
- 推送到 GitHub 仓库后，GitHub Pages 会自动构建和部署
- 访问地址：https://firelifes.github.io
